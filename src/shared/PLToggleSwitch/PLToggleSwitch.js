import React from "react";
import Switch from "@material-ui/core/Switch";

const PLToggleSwitch = (props) => {
    return (
        <Switch {...props} color={"primary"} />
    )

};

export default PLToggleSwitch